/**
 * 该包下的类都为数据库表对应的实体类，可用以记录一些通用的游戏数据，以供一些简单的游戏直接使用。<br>
 * 每个类的字段名称和顺序应该严格与表相对应，即这些类都是没有任何逻辑的JavaBean；<br>
 * 如果有时候需要增加字段，请单独写对应的VO、BO或DTO类，而不要在实体类中增加字段
 *
 * @author fagarine
 */
package cn.laoshini.dk.entity;